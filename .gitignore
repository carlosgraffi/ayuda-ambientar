node_modules/
.next/
out/
.wrangler/
next-env.d.ts
*.tsbuildinfo
.env*.local
.DS_Store

# Clon del repo viejo (carlosgraffi/PatagoniaFires), solo para consulta.
# Nunca se compila ni se importa desde acá.
reference/

# Lo escribe scripts/fetch-focos.mjs en cada build, con la key del entorno.
supabase/.temp/
public/data/focos-*.geojson

# Estado local del stack de Supabase (Docker).
supabase/.temp/
supabase/.branches/

# Variables locales de Wrangler (claves de servicio).
.dev.vars

# Claves locales. .env.example tiene la plantilla.
.env.local

# Lo escribe scripts/write-public-config.mjs en cada build.
public/config.json
